name: authentication_repository
description: A Authentication Repository
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  http_client_handler:
    path: ../http_client_handler
  secure_storage_helper:
    path: ../secure_storage_helper
